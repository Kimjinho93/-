#include <string>
#include <vector>

using namespace std;

int solution(int a, int b) {
    int answer = 0;
    string answer_1 = to_string(a) + to_string(b);
    string answer_2 = to_string(b) + to_string(a);
    
    return max(stoi(answer_1), stoi(answer_2));
}